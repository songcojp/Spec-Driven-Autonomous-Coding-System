# Owner Analysis: FEAT-011 Codex Review Pass 20

Decision: fix-required

Fix-now:
- P1 Preserve legacy approved reviews when enforcing terminal gates. Actionable compatibility issue: migrated pre-v9 approved `review_items` may have no `approval_records`, so the terminal approval gate must treat an approved review with no approval rows as a legacy positive approval instead of blocking persisted `done`/`delivered` state.
- P2 Restore the parent feature to its paused lifecycle stage. Actionable lifecycle issue: resolving a task-level review can collapse a parent feature from `tasked` to `implementing`; parent recomputation should fall back to the captured paused feature status.

No-action:
- none

Needs clarification:
- none

Review quality judgment:
- Useful and in scope. Both findings are correctness/compatibility risks covered by FEAT-011 lifecycle and migration requirements.
