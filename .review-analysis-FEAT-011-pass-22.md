# Owner Analysis: FEAT-011 Codex Review Pass 22

Decision: fix-required

Fix-now:
- P1 Keep approval-needed status reviews open on clean reruns. Actionable approval-gate issue: clean evidence can close remediated risk/clarification reviews, but must not silently close `approval_needed` reviews that require an explicit human decision.
- P1 Recompute parent feature out of done when a reviewed task reopens. Actionable lifecycle issue: if a terminal feature pauses for task review and the task is sent back to active work, the feature must leave `done`/`delivered` instead of preserving a terminal paused state.

No-action:
- none

Needs clarification:
- none

Review quality judgment:
- Useful and in scope. Both findings protect FEAT-011 approval integrity and lifecycle consistency.
